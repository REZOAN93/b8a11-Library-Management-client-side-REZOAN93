import React from 'react';
import Header from '../Header/Header';

const Borrowed = () => {
    return (
        <div>
            <Header></Header>
            <h1>This is Borrowed page</h1>
        </div>
    );
};

export default Borrowed;